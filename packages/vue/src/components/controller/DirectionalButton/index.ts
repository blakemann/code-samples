import DirectionalButton, { Direction, ComponentEvent } from './DirectionalButton.vue';

export default DirectionalButton;

export {
  Direction as DirectionalButtonDirection,
  ComponentEvent as DirectionalButtonEvent,
};
